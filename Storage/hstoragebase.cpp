#include "hstoragebase.h"

#include <QStringList>

#include "../Common/hjsoncommon.h"
// int HStorageJsonObject::save(QJsonObject& d) {
//  for (const auto& k : keys()) {
//    d.insert(k, value(k));
//  }
//  return 0;
//}

// int HStorageJsonObject::load(const QJsonObject& o) {
//  for (const auto& k : o.keys()) {
//    insert(k, o.value(k));
//  }
//  return 0;
//}

// int HStorageJsonObject::save(const QString& path) {
//  return HJsonCommon::writeJson(path, *this);
//}

// int HStorageJsonObject::load(const QString& path) {
//  return HJsonCommon::readJsonObject(path, *this);
//}
